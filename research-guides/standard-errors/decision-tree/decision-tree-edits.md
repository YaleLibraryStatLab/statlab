# Standard Errors Decision Tree — Master Edit List

Consolidated from three independent review passes:
- **First pass:** Neitzche (statistician) + Nash (econometrician) — full review of `decisions.js`, `app.jsx`, `Standard Errors Decision Tree.html`.
- **Second pass:** two independent Codex reviewers.
- **Adjudication:** direct source inspection + targeted re-review by Neitzche/Nash.

Each edit lists a **paste-ready fix** and an **agent proposal** (persona / model / prompt) for cases where additional scrutiny — verifying a package exists, finding cleaner code, or confirming a method's implementation — will yield better insight. Where the edit is already fully specified and verified, it is marked **Direct apply — no agent needed**.

---

## Cross-cutting rules (apply to every item)

1. **Duplication.** Every content fix lands in **two places**: `decisions.js` **and** its verbatim twin inside the `<script>` block of `Standard Errors Decision Tree.html` (~+508 line offset for `decisions.js` content, ~+1248 for `app.jsx`). If you only edit the source files, the rendered iframe keeps the old text.
2. **Glossary LaTeX caveat.** The Tooltip renders `entry.definition` as a **raw string, not through `MathText`** (`app.jsx:64-71`). `$…$` pasted into a glossary entry shows literal dollar signs. Recommended enabler: wrap `tip-def` in `<MathText text={entry.definition} />` (see T2-6). Rec bodies/checks already route through `MathText`.

**Model legend:** `opus` = hardest judgment / method correctness; `sonnet` = package/API verification with web search; `haiku` = mechanical text/code application; `fable` = fast drafting.

---

## ✅ APPLICATION STATUS — applied to `decisions.js`, `app.jsx`, and the HTML twin

All edits below were applied directly to `decisions.js` and `app.jsx`, then the HTML twin's two `<script>` blocks were regenerated by splicing in the edited sources (no manual duplicate edits). `decisions.js` passes `node --check`; `app.jsx` has balanced braces/parens (full JSX transpile happens via the page's runtime Babel).

- **Tier 0 — ALL APPLIED:** T0-1 (CR2 formula), T0-2 (Stata `vce(hc3)`), T0-3 (CCV closed-form + TSCB pseudocode, 4 blocks), T0-4 (spatial `mgcv::te()` + manual BIC + patsy + acreg; primary updated), T0-5 (Neyman SE + test-inversion CI + Python).
- **Tier 1 — ALL APPLIED** except the per-rec `resources` half of T1-13: T1-1 (survey-design branch + rec + coverage desc), T1-2 (`spatial_structure` step), T1-3 (panel→cluster route + hc23_pop check), T1-4 (crossed example), T1-5 (two-way asymptotics), T1-6 (statsmodels Python), T1-7 (HAC `nobs()` + caveats), T1-8 (clubSandwich DoF/cluster; headline+primary reconciled), T1-9 (full-pop framing + EHW), T1-10 (HC2/HC3 formula + glossary), T1-11 (folded into T0-5), T1-12 (Moulton inflation), **T1-13 footer sources added** *(per-recommendation `resources` arrays DEFERRED — research/curation task)*.
- **Tier 2 — APPLIED:** T2-1 (stepper terminal dot), T2-2 (nested `<button>` → `role="button"` div), T2-3 (within=LSDV), T2-4 (delete S−1 ×2), T2-5 (convenience grouping), T2-6 (glossary via `MathText`), T2-8 (`exogeneous`→`exogenous`, `endogeneous`→`endogenous`, both files), T2-9 (`impose_null=TRUE`), T2-10 (sourced rate), T2-11 (Rademacher $2^{G-1}$), T2-12 (Conley–Kelly comparator), T2-13 (`MathText` `$$…$$` hardening), T2-14 (Moulton 1986), T2-16 (drop judges example), T2-17 (Tooltip hook order), T2-18 (KatexMath dead try/catch removed), T2-20 (en-dashes), T2-21 (`set.seed` note).
- **DEFERRED (not applied):** T1-13 per-rec resources (curation); T2-7 (extra CSS styling for pseudocode — env tags already read "NOT runnable"); T2-15 (surface dead `crve`/`hc2_hc3` glossary entries — ambiguous tooltip targets, low value); T2-19 (build pipeline to drop runtime Babel — architectural, changes the delivery model).

---

## 🔴 TIER 0 — Critical (wrong math / broken code / dead-ends)

### T0-1 · CR2 formula is mathematically wrong
- **Location:** `decisions.js:505` (`crve_cr2.body`)
- **Source:** Neitzche + Nash + Codex (3-way convergence)
- **Edit:** replace the body with:
  > "Standard CRVE under-covers in this range. The CR2 (Bell–McCaffrey) estimator pre-multiplies each cluster's residual vector $\hat{\mathbf{u}}_g$ (length $n_g$) by a cluster-specific symmetric matrix $\mathbf{A}_g$: $\tilde{\mathbf{u}}_g = \mathbf{A}_g\hat{\mathbf{u}}_g$, where $\mathbf{A}_g$ satisfies $\mathbf{A}_g(\mathbf{I}_{n_g} - \mathbf{H}_{gg})\mathbf{A}_g = \mathbf{I}_{n_g} - \mathbf{H}_{gg}$ and $\mathbf{H}_{gg} = \mathbf{X}_g(\mathbf{X}'\mathbf{X})^{-1}\mathbf{X}_g'$ is the cluster's block of the hat matrix. It reduces exactly to the HC2 weight $(1-h_{ii})^{-1/2}$ when each cluster is one observation. Inference uses $t$ with Bell–McCaffrey/Satterthwaite DoF, not $G-1$ blindly."
- **Agent:** Direct apply — no agent needed. (Neitzche verified twice; formula is standard Bell–McCaffrey 2002 / clubSandwich.)

### T0-2 · Stata HC2/HC3 snippet delivers HC1
- **Location:** `decisions.js:362`
- **Source:** Neitzche + Nash + Codex
- **Edit:** `["Stata", "regress y x, vce(hc3)   // vce(robust) is HC1; use hc2 or hc3 explicitly"]`
- **Agent:** Direct apply — no agent needed. (`regress, vce(hc2|hc3)` is documented Stata syntax; matches `standard-errors.qmd:373`.)

### T0-3 · CCV/TSCB ships unrunnable pseudocode (phantom helpers, no Python)
- **Location:** `decisions.js:410-455` (`ccv_tscb.code`)
- **Source:** Neitzche + Nash + Codex
- **Edit:** replace the entire `code:` array with the four-block version (CCV closed-form runnable in R + Python; full two-stage TSCB labeled NOT runnable with empty-cell guards). Full block:

```
code: [
  ["R — closed form (runnable)", `# CCV (Causal Cluster Variance), Abadie-Athey-Imbens-Wooldridge (2023, QJE).
# Combine cluster-robust and heteroskedastic-robust variances, discounting the
# cluster term by the sampling fraction rho = fraction of each cluster's target
# population you observe (rho -> 1 means you nearly observe the whole cluster).
library(sandwich)
V_cluster <- vcovCL(fit, cluster = df$g)   # Liang-Zeger
V_robust  <- vcovHC(fit, type = "HC2")     # heteroskedastic-robust
V_ccv     <- V_robust + (1 - rho) * (V_cluster - V_robust)
se_ccv    <- sqrt(diag(V_ccv))[target]     # use t(G - 1) critical values`],

  ["Python — closed form (runnable)", `# CCV closed form, AAIW (2023). statsmodels gives both vcovs directly.
import numpy as np, statsmodels.formula.api as smf
fit_cl = smf.ols('y ~ x', data=df).fit(cov_type='cluster', cov_kwds={'groups': df['g']})
fit_hc = smf.ols('y ~ x', data=df).fit(cov_type='HC2')
rho    = 0.1   # fraction of each cluster's target population observed
V_ccv  = fit_hc.cov_params() + (1 - rho) * (fit_cl.cov_params() - fit_hc.cov_params())
se_ccv = np.sqrt(np.diag(V_ccv))[list(fit_hc.params.index).index('x')]  # t(G-1) crit vals`],

  ["R — TSCB pseudocode (NOT runnable)", `# Two-Stage Cluster Bootstrap, AAIW (2023, QJE). Conceptual sketch, not code.
# ASSUMPTION: sampling fraction rho per cluster is known or defensible.
#   rho ~ 0  -> behaves like standard cluster bootstrap (sampling uncertainty)
#   rho ~ 1  -> design uncertainty dominates; Stage-1 cluster resampling shrinks.
# Requires WITHIN-cluster treatment variation (treated AND control units per cluster).

theta_hat <- coef(fit_fun(df))[target]
clusters  <- unique(df$g)
G         <- length(clusters)

theta_star <- numeric(B)
for (b in 1:B) {
  # ---- STAGE 1: resample clusters (scaled by 1 - rho) ----
  drawn <- sample(clusters, size = G, replace = TRUE)

  boot_rows <- list()
  for (i in seq_along(drawn)) {
    g0      <- drawn[i]
    rows_g  <- df[df$g == g0, ]
    treated <- rows_g[rows_g$d == 1, ]
    control <- rows_g[rows_g$d == 0, ]

    # ---- GUARD: skip clusters with an empty treated or control cell ----
    if (nrow(treated) == 0 || nrow(control) == 0) next

    # ---- STAGE 2: resample units within the cluster, PRESERVING the split ----
    n_t <- nrow(treated); n_c <- nrow(control)
    samp_t <- treated[sample(n_t, n_t, replace = TRUE), ]
    samp_c <- control[sample(n_c, n_c, replace = TRUE), ]
    block  <- rbind(samp_t, samp_c)
    block$g_boot <- paste0("bootclust_", i)
    boot_rows[[length(boot_rows) + 1]] <- block
  }

  if (length(boot_rows) < 2) { theta_star[b] <- NA; next }

  boot          <- do.call(rbind, boot_rows)
  theta_star[b] <- coef(fit_fun(boot))[target]
}

theta_star <- theta_star[!is.na(theta_star)]
se_tscb    <- sd(theta_star)
ci_tscb    <- quantile(theta_star, c(0.025, 0.975))
# Reference implementations: Stata 'ccv'/'tscb' (Fernandez-Val, Imbens et al.);
# AAIW (2023) QJE replication archive.`],

  ["Python — TSCB pseudocode (NOT runnable)", `# Two-Stage Cluster Bootstrap, AAIW (2023). Conceptual sketch, not code.
import numpy as np, pandas as pd

theta_hat = fit_fun(df)[target]
clusters  = df['g'].unique()
G         = len(clusters)
theta_star = []

for b in range(B):
    drawn = np.random.choice(clusters, size=G, replace=True)   # STAGE 1
    blocks = []
    for i, g0 in enumerate(drawn):
        rows_g  = df[df['g'] == g0]
        treated = rows_g[rows_g['d'] == 1]
        control = rows_g[rows_g['d'] == 0]
        if len(treated) == 0 or len(control) == 0:             # GUARD
            continue
        samp_t = treated.sample(len(treated), replace=True)     # STAGE 2
        samp_c = control.sample(len(control), replace=True)
        block  = pd.concat([samp_t, samp_c]); block['g_boot'] = f'bootclust_{i}'
        blocks.append(block)
    if len(blocks) < 2:
        continue
    theta_star.append(fit_fun(pd.concat(blocks))[target])

theta_star = np.array(theta_star)
se_tscb    = theta_star.std(ddof=1)
ci_tscb    = np.quantile(theta_star, [0.025, 0.975])`],
]
```
- **Agent (recommended — verify the reference pointer before publishing):**
  - **Persona:** Nash (econometric wizard; refuses to ship a resource he can't stand behind).
  - **Model:** `sonnet` (web search needed).
  - **Prompt:** "Verify the existence and current names of: (1) a Stata package implementing AAIW (2023) CCV/TSCB — confirm whether it is called `ccv`/`tscb`, who authors it, and the install command; (2) the AAIW (2023, QJE) replication archive (QJE Dataverse / author site) and give the canonical URL/DOI. If the Stata package name is wrong or doesn't exist, give the correct pointer or tell me to drop the reference. Do NOT invent a package. Return exact, citable strings to paste into the rec body."

### T0-4 · Spatial `%x%` Kronecker bug + undefined `k_bic`
- **Location:** `decisions.js:597` (`spatial.code`), update `spatial.primary` (line 594-595)
- **Source:** Neitzche + Nash + Codex
- **Edit:** replace `spatial.code` with the four blocks (mgcv `te()` recommended; manual row-wise tensor + explicit BIC; Python patsy; Stata `acreg`):

```
code: [
  ["R — mgcv tensor smooth (recommended, runnable)", `# Step 1: de-trend with a 2-D spatial smooth; df/penalty chosen by REML/BIC.
library(mgcv)
fit <- gam(y ~ x + te(lat, lon, k = 10), data = d, method = "REML")
# te() builds the row-wise tensor-product basis correctly and penalizes wiggliness.
# Step 2: few-cluster-robust spatial SEs on k-medoids partitions.
g <- cluster::pam(cbind(d$lat, d$lon), k = 6)$clustering
clubSandwich::coef_test(fit, vcov = "CR2", cluster = g)`],

  ["R — manual tensor basis + explicit BIC (runnable)", `# Build a ROW-WISE tensor product, not %x% (Kronecker).
library(splines)
Blat <- bs(d$lat, df = K); Blon <- bs(d$lon, df = K)
B <- do.call(cbind, lapply(seq_len(ncol(Blat)), function(j) Blat[, j] * Blon))
pc <- prcomp(B, scale. = TRUE)
bic <- sapply(seq_len(ncol(pc$x)), function(k)
         BIC(lm(d$y ~ d$x + pc$x[, 1:k, drop = FALSE])))
k_bic <- which.min(bic)
fit <- lm(y ~ x + pc$x[, 1:k_bic], data = d)
g <- cluster::pam(cbind(d$lat, d$lon), k = 6)$clustering
clubSandwich::coef_test(fit, vcov = "CR2", cluster = g)`],

  ["Python — spatial spline de-trend + Conley/HAC", `import numpy as np, pandas as pd, statsmodels.formula.api as smf
from patsy import dmatrix
B = dmatrix("bs(lat, df=5):bs(lon, df=5)", d, return_type="dataframe")
Xd = pd.concat([d[['y','x']].reset_index(drop=True), B.reset_index(drop=True)], axis=1)
fit = smf.ols("y ~ x + " + " + ".join(B.columns.map(lambda c: f'Q(\"{c}\")')), data=Xd).fit()
# Step 2: Conley/Kelejian-Prucha spatial HAC via spreg, or k-medoids cluster SEs.`],

  ["Stata — Conley via acreg (runnable)", `* ssc install acreg
acreg y x, lat(lat) lon(lon) dist(500) spherical
* Add spatial controls (polynomials in lat/lon) before acreg for de-trending.`],
]
```
- **Agent (recommended — verify the code runs and the APIs are current):**
  - **Persona:** Neitzche (stickler for clean, correct, runnable code).
  - **Model:** `sonnet` (may run R/Python to smoke-test; web search for API).
  - **Prompt:** "Smoke-test these four spatial snippets for API correctness (do NOT need real data — check that calls are valid). Confirm: (1) `mgcv::gam` with `te(lat, lon, k=10)` and `method='REML'` is correct; (2) `clubSandwich::coef_test` accepts an `mgcv` gam object and a vector `cluster` — if NOT (clubSandwich may only support lm/plm/etc.), tell me and propose the correct combination; (3) the manual row-wise tensor via `lapply(... Blat[,j] * Blon)` produces an n×(K·K) matrix; (4) patsy `bs(lat,df=5):bs(lon,df=5)` interaction syntax is valid and the `Q()` quoting works; (5) `acreg` option names (`lat/lon/dist/spherical`). Flag any call that won't run and give the corrected line. This is the highest-risk code in the tool — be skeptical."

### T0-5 · `cluster_randomization_pop` ships a sharp-null test, not an SE/CI
- **Location:** `decisions.js:472` (`cluster_randomization_pop.code`)
- **Source:** Codex (Nash confirmed valid)
- **Edit:** replace `code:` with three blocks (design-based Neyman SE; CI by test inversion; Python manual):

```
code: [
  ["R — design-based SE (cluster-level Neyman)", `# Aggregate to one value per cluster, then difference-in-means with the
# Neyman (conservative) cluster-level variance.
agg <- aggregate(y ~ cluster_id + z, data = df, FUN = mean)
yt  <- agg$y[agg$z == 1]; yc <- agg$y[agg$z == 0]
tau_hat   <- mean(yt) - mean(yc)
se_neyman <- sqrt(var(yt)/length(yt) + var(yc)/length(yc))   # conservative
ci <- tau_hat + c(-1, 1) * qt(0.975, df = length(yt) + length(yc) - 2) * se_neyman
# Design-aware alternative:
# estimatr::difference_in_means(y ~ z, blocks = block_id, clusters = cluster_id, data = df)`],

  ["R — CI by test inversion (randomization inference)", `# Invert the RI test over a grid; the CI is the set of tau0 NOT rejected at 5%.
library(ri2)
decl <- declare_ra(clusters = df$cluster_id, m = treated_clusters)
grid <- seq(-2, 2, by = 0.02)
keep <- sapply(grid, function(tau0) {
  df$y_adj <- df$y - tau0 * df$z
  out <- conduct_ri(y_adj ~ z, declaration = decl, assignment = "z",
                    sharp_hypothesis = 0, data = df)
  summary(out)$two_tailed_p_value > 0.05
})
ci_ri <- range(grid[keep])`],

  ["Python — no clean RI package; manual design-based SE", `import numpy as np, pandas as pd
agg = df.groupby(['cluster_id', 'z'])['y'].mean().reset_index()
yt = agg.loc[agg.z == 1, 'y'].values; yc = agg.loc[agg.z == 0, 'y'].values
tau_hat   = yt.mean() - yc.mean()
se_neyman = np.sqrt(yt.var(ddof=1)/len(yt) + yc.var(ddof=1)/len(yc))
# Test-inversion RI: loop tau0, permute cluster-level z under the actual design,
# recompute tau on y - tau0*z, keep tau0 not rejected at 5%.`],
]
```
- **Agent (recommended — verify ri2/estimatr API):**
  - **Persona:** Nash.
  - **Model:** `sonnet` (web search / run R).
  - **Prompt:** "Confirm the `ri2` and `estimatr` APIs in this snippet: (1) does `ri2::conduct_ri` return an object whose `summary()` exposes `two_tailed_p_value` (check the exact field name)? (2) Is `declare_ra(clusters=, m=)` the correct `randomizr` signature for cluster randomization? (3) Is `estimatr::difference_in_means(y ~ z, blocks=, clusters=)` valid and does it return Neyman SEs? Correct any field/argument names. Confirm the test-inversion logic is statistically sound for a cluster-randomized CI."

---

## 🟠 TIER 1 — Major

### T1-1 · Add complex survey-design branch
- **Location:** `decisions.js:138` (`sampling` step) + `:117` + new `RECS.survey_design`
- **Source:** Codex (Nash recommends dedicated branch)
- **Edit:**
  (a) Add option to `sampling`:
  ```
  { id: "complex_survey", label: "Complex survey design (weights, strata, PSUs)",
    desc: "CPS, PSID, NHANES, DHS — a published sampling design with weights, stratification, and primary sampling units.",
    terminal: "survey_design", flag: "sampled_clusters" },
  ```
  (b) Fix `coverage.random_sample.desc` (line 117): remove "CPS, PSID, household surveys" → "a simple random sample from a large population frame."
  (c) Add `RECS.survey_design`:
  ```
  survey_design: {
    headline: "Survey-design (Horvitz–Thompson) variance estimation",
    tagline: "Published complex survey with weights, strata, and PSUs.",
    body: "When the data come from a documented probability sample (CPS, PSID, NHANES, DHS), inference must use the survey design: probability weights, stratification, and primary sampling units. Plain HC or one-way cluster SEs ignore stratification (which shrinks variance) and unequal weighting (which can inflate or deflate it). Use a survey-design variance estimator with the published design variables.",
    primary: "Taylor-linearization or replicate-weight variance using the survey's weight, strata, and PSU variables. Do not substitute ad hoc clustering for the documented design.",
    code: [
      ["R", `library(survey)
des <- svydesign(ids = ~psu, strata = ~stratum, weights = ~wt, data = df, nest = TRUE)
fit <- svyglm(y ~ x, design = des)
summary(fit)   # design-based SEs`],
      ["Stata", `svyset psu [pweight = wt], strata(stratum)
svy: regress y x`],
      ["Python", `# statsmodels has limited complex-survey support; use samplics for full
# Taylor-linearization / replicate-weight variance.
from samplics.estimation import TaylorEstimator
est = TaylorEstimator(parameter="mean")
est.estimate(df["y"], samp_weight=df["wt"], stratum=df["stratum"], psu=df["psu"])
# Regression with survey weights: statsmodels WLS gives point estimates but NOT
# design-correct SEs; prefer R survey or Stata svy.`],
    ],
    checks: [
      "Use the design's own replicate weights (BRR/jackknife) when supplied — they encode confidentiality-protected design detail linearization cannot.",
      "Stratification typically REDUCES variance; ignoring it makes SEs too large. Clustering/PSUs typically INCREASE it. The net effect is the design effect (DEFF) — report it.",
      "Subpopulation analysis must use svyby / subpop options, not a filtered dataset — dropping rows breaks the variance estimator.",
      "If you only have weights (no strata/PSU), weighted regression with HC SEs is a fallback, but state that you could not use the full design.",
    ],
  },
  ```
- **Agent (recommended — verify survey package APIs across 3 languages):**
  - **Persona:** Nash.
  - **Model:** `sonnet` (web search).
  - **Prompt:** "Verify: (1) `survey::svydesign(ids=~psu, strata=~stratum, weights=~wt, nest=TRUE)` + `svyglm` is the correct idiom; (2) Stata `svyset psu [pweight=wt], strata(stratum)` then `svy: regress`; (3) the `samplics` Python package exists, is maintained, and `TaylorEstimator(parameter='mean').estimate(...)` has that signature — if `samplics` is unmaintained or the API differs, recommend the best current Python option for design-based survey variance (or state honestly there isn't one). Return corrected snippets."

### T1-2 · Spatial panel never asked about time
- **Location:** `decisions.js:99-104` (route) + new `spatial_structure` step
- **Source:** Codex (Nash)
- **Edit:** change `spatial` option from `terminal: "spatial"` to `next: "spatial_structure"`; add:
  ```
  spatial_structure: {
    n: 2, short: "Spatial structure",
    title: "Is your spatial data a single cross-section or a spatial panel?",
    why: "Spatial panels carry BOTH cross-sectional spatial autocorrelation and within-unit serial correlation over time. De-trend spatially first, then handle the temporal dimension separately.",
    options: [
      { id: "spatial_cross", label: "Spatial cross-section",
        desc: "One observation per location at a single point in time.", terminal: "spatial" },
      { id: "spatial_panel", label: "Spatial panel",
        desc: "Locations observed over multiple periods.", terminal: "spatial", flag: "panel" },
    ],
  },
  ```
- **Agent:** Direct apply — no agent needed. (Pure routing; `flag: "panel"` reuses existing addendum logic.) **Note:** verify step `n:` numbering doesn't collide visually with the cross/panel branch (both use `n:2`); the Stepper is index-based so it's cosmetic.

### T1-3 · Panel + full-population routing dead-ends at HC2/HC3
- **Location:** `decisions.js:259-264` (`assignment_pop.rand_individual`) + `hc23_pop.checks`
- **Source:** Codex (confirmed in code; Nash confirmed destination)
- **Edit:** add `routes: [{ ifAnyFlag: ["panel"], next: "cluster_dimensions" }]` to the option; add to `hc23_pop.checks`:
  > "If your data are a panel (units observed over time), do not stop at HC2/HC3 — serial correlation within units remains even at full population coverage. Cluster by the unit, or use the two-way path if time shocks also matter."
- **Agent:** Direct apply — no agent needed. (Mirrors the existing `assignment.rand_individual` route; logic verified in `app.jsx:92,109-118`.)

### T1-4 · Two-way clustering example is nested, not crossed
- **Location:** `decisions.js:305` (`cluster_dimensions.two_way.desc`)
- **Source:** Codex (Nash confirmed)
- **Edit:**
  > "Shocks correlated within units over time AND within cross-sectional groups in the same period (e.g., panel by firm and by year; patents cited by both citing-firm and technology-class; students clustered by both school and neighborhood when the two cross-cut)."
- **Agent:** Direct apply — no agent needed.

### T1-5 · Two-way CRVE asymptotics overstated
- **Location:** `decisions.js:556`, `:563`
- **Source:** Neitzche + Codex
- **Edit:** body → "consistency is driven by the smaller of the two, i.e. $\min(G_A,G_B)\to\infty$; each dimension having ~30+ groups is a rough adequacy guide." Soften final sentence → "The number of intersection cells $G_{A\times B}$ is typically large and is **not** what protects you — small $\min(G_A,G_B)$ is the binding constraint." Describe $t(\min(G_A,G_B)-1)$ as a heuristic.
- **Agent:** Direct apply — no agent needed.

### T1-6 · Python generic-clustering example uses `PanelOLS`
- **Location:** `decisions.js:488-490`
- **Source:** Nash + Codex
- **Edit:**
  ```
  ["Python", "import statsmodels.formula.api as smf\nfit = smf.ols('y ~ x', data=df).fit(\n    cov_type='cluster', cov_kwds={'groups': df['design_unit']})\nprint(fit.summary())\n# Panel with entity FE: linearmodels.PanelOLS(..., cov_type='clustered', cluster_entity=True)"]
  ```
- **Agent:** Direct apply — no agent needed. (Matches `standard-errors.qmd:461-465`; statsmodels `cov_type='cluster'` is stable.)

### T1-7 · HAC bare `T` → silent `lag=0`; missing caveats
- **Location:** `decisions.js:578`, `:583`
- **Source:** Neitzche + Nash + Codex
- **Edit:** `R` → `lmtest::coeftest(fit, vcov = sandwich::NeweyWest(fit, lag = floor(0.75 * nobs(fit)^(1/3)), prewhite = FALSE))`. Checks: distinguish $4(T/100)^{2/9}$ (plug-in-free) vs $0.75\,T^{1/3}$ (rate-optimal); add "HAC does not fix nonstationarity or misspecified dynamics; Driscoll–Kraay is not a generic small-T panel fix."
- **Agent (light — confirm NeweyWest defaults):**
  - **Persona:** Neitzche.
  - **Model:** `haiku`.
  - **Prompt:** "Confirm `sandwich::NeweyWest` prewhitens by default (so `prewhite=FALSE` changes the result) and that wrapping in `lmtest::coeftest(fit, vcov=NeweyWest(...))` is the idiomatic display call. One-paragraph answer."

### T1-8 · clubSandwich mislabels DoF + omits `cluster` arg
- **Location:** `decisions.js:525`, reconcile `:519`
- **Source:** Neitzche + Nash + Codex
- **Edit:** `clubSandwich::coef_test(fit, vcov = "CR2", cluster = ~ g)  # Satterthwaite (Bell-McCaffrey) DoF`. Drop/justify the "Imbens–Kolesár DoF" label in the headline at line 519.
- **Agent:** Direct apply — no agent needed. (Neitzche verified `coef_test` defaults to Satterthwaite.)

### T1-9 · EHW "upper bound" + full-population framing overstated
- **Location:** `decisions.js:124`, `:392`
- **Source:** Neitzche (M9) + Codex (Neitzche provided combined fix)
- **Edit:**
  (a) coverage desc (124): "All 50 states, all EU countries, all districts in a country — you observe the entire target population, so $\rho \to 1$. (If you actually want to generalize to other periods or conditions, these are a sample from a superpopulation and this branch does not apply.)"
  (b) `hc23_pop.body` (392): "If your estimand is the effect *within this observed finite population*, ordinary sampling uncertainty is essentially zero and the only remaining uncertainty is design-based: which potential outcome was revealed for each unit. (If instead you want to generalize to a broader superpopulation, sampling-type uncertainty does not vanish — treat this as a sampling problem, not a full-population one.) Under that design-based view, EHW robust SEs are generally conservative for the average treatment effect (Abadie, Athey, Imbens & Wooldridge 2020); they coincide with the design-based variance only when effects are constant."
- **Agent:** Direct apply — no agent needed.

### T1-10 · HC2/HC3 missing complete formulas
- **Location:** `decisions.js:357` (body), `:33` (glossary)
- **Source:** Codex (Neitzche provided)
- **Edit:**
  (a) `hc23.body` append: " The estimator is the Eicker–Huber–White sandwich $\hat V = (X'X)^{-1}\!\left(\sum_i \omega_i\,\hat u_i^2\, x_i x_i'\right)\!(X'X)^{-1}$ with $\omega_i = 1/(1-h_{ii})$ for HC2 and $\omega_i = 1/(1-h_{ii})^2$ for HC3, where $h_{ii}=x_i'(X'X)^{-1}x_i$ is leverage."
  (b) Glossary: if T2-6 applied, use full LaTeX; else Unicode: "...weight is ωᵢ = 1 (HC0), n/(n−p) (HC1), 1/(1−hᵢᵢ) (HC2), 1/(1−hᵢᵢ)² (HC3), where hᵢᵢ is observation i's leverage."
- **Agent:** Direct apply — no agent needed. (Weights confirmed by Neitzche.)

### T1-11 · `randomizr`/`ri2` snippet errors (`z` undefined)
- **Location:** `decisions.js:472`
- **Source:** Nash (first pass) — **superseded by T0-5 rewrite.** Ensure the T0-5 replacement retains data-contract comments.
- **Agent:** Folded into T0-5.

### T1-12 · Unsourced "20×" SE inflation
- **Location:** `decisions.js:358`
- **Source:** Neitzche + Nash + Codex
- **Edit:** "can inflate your standard errors substantially — by an order of magnitude when within-cluster covariate correlation is high — for no valid design reason (the Moulton inflation $\sqrt{1+(\bar n_g-1)\rho_x\rho_u}$; Abadie et al. 2023)."
- **Agent:** Direct apply — no agent needed.

### T1-13 · Per-recommendation resources (footer-only insufficient)
- **Location:** `app.jsx:366` + rec objects
- **Source:** Neitzche + Nash + Codex
- **Edit:** add a `resources` field to each rec (title, authors/year, DOI/package link, one-line "why read") and render it; add missing footer sources: Bell & McCaffrey (2002), Cameron–Gelbach–Miller (2011), Bertrand–Duflo–Mullainathan (2004), Newey–West (1987/1994), Andrews (1991), Webb (2014).
- **Agent (recommended — this is a research + design task):**
  - **Persona:** Nash (only shares resources he can stand behind).
  - **Model:** `opus` (judgment about resource quality + DOI lookups).
  - **Prompt:** "For each recommendation in `decisions.js` RECS, propose 1–3 best-in-class resources a researcher should actually read, with full citation + DOI/stable URL + a one-line 'why read this.' Cross-check against `standard-errors.bib` (reuse those entries where possible). Prioritize canonical, high-quality sources over convenience. Then propose the minimal `app.jsx` render change to display a per-rec `resources` array. Do not pad — quality over quantity."

---

## 🟡 TIER 2 — Minor

| # | Item | Location | Fix | Source | Agent |
|---|------|----------|-----|--------|-------|
| T2-1 | Stepper terminal dot missing when `visited.length ≥ 6` | `app.jsx:181-184` | Loop to `Math.max(visibleSteps.length + (terminal ? 1 : 0), minDots)` | Codex | Direct apply (`haiku` if desired) |
| T2-2 | Nested `<button>` (tooltip inside answer) — invalid HTML | `app.jsx:205`, `:56` | Move Tooltip outside `.ans` button (sibling control) | Codex | **Agent:** Neitzche / `sonnet` — "Refactor so the glossary `?` button is not nested inside the answer `<button>` (invalid HTML) while preserving click-to-open and stopPropagation. Give the JSX diff for `Question` + `Tooltip`." |
| T2-3 | Within vs LSDV framed as estimator choice | `decisions.js:651` | They're algebraically equivalent; issue is variance/DoF correction — reword | Codex | Direct apply |
| T2-4 | Undefined "S − 1" | `decisions.js:37`, `:661` | Delete "(or S − 1)" / "(or $S-1$)" both spots | Codex (Neitzche: delete) | Direct apply |
| T2-5 | Convenience-sample dependence caveat | `decisions.js:619` | Add check: "If the convenience sample still has internal grouping … cluster at that level instead of HC2/HC3. … HC2/HC3 is a floor, not a ceiling." | Codex (Nash) | Direct apply |
| T2-6 | Glossary tooltips don't route through `MathText` | `app.jsx:64-71` | Wrap `tip-def` in `<MathText>` (enabler for T1-10b) | Neitzche (new) | **Agent:** Neitzche / `haiku` — "Wrap the glossary `tip-def` content in `<MathText>` and confirm no other glossary string breaks when `$` is interpreted as math. List any glossary entry containing a literal `$`." |
| T2-7 | Pseudocode vs runnable not visually distinct | `app.jsx:252-255` | Env tags now say "(NOT runnable)"; optionally style differently | Codex | Direct apply |
| T2-8 | Misspelled `exogeneous`/`endogeneous` ids | `decisions.js:19,23`, `app.jsx:233` | Rename to `exogenous`/`endogenous` everywhere | all passes | **Agent:** Neitzche / `sonnet` — "Rename `exogeneous`→`exogenous` and `endogeneous`→`endogenous` across `decisions.js`, `app.jsx`, and the HTML twin (keys, ids, flags, tooltip arrays, `flags.has(...)` checks). Verify no string is missed and the flag matching still works. Give a full grep-verified diff." |
| T2-9 | `impose_null = TRUE` explicit in R wild bootstrap | `decisions.js:525` | Add it (already the default — explicitness only) | Codex | Direct apply |
| T2-10 | Over-precise rejection rates w/o DGP | `decisions.js:521,336,493,602` | Prefix "can"/"in [author]'s simulations" | Neitzche | Direct apply |
| T2-11 | Rademacher $2^G$ vs usable $2^{G-1}$ | `decisions.js:539` | 32 distinct two-sided stats at G=6, not 64 | Neitzche | Direct apply |
| T2-12 | Conley–Kelly "38%" needs comparator | `decisions.js:593` | "with conventional SEs … versus the 5% a valid test would give" | Neitzche | Direct apply |
| T2-13 | `MathText` regex can't handle `$$…$$`/escaped `\$` | `app.jsx:21` | Harden to `/(\$\$[^$]+\$\$|\$[^$]+\$)/g` + display branch | Neitzche | **Agent:** Neitzche / `sonnet` — "Harden `MathText` to support `$$…$$` display math and escaped `\$`, with a display-mode branch into `KatexMath`. Provide the full component + a couple of test strings." |
| T2-14 | Moulton factor misattributed to Jackson (2019) | `decisions.js:57` | It's Moulton (1986); Jackson formalizes covariate-clustering as bias driver | Nash | Direct apply |
| T2-15 | Dead glossary entries `crve`/`hc2_hc3` | `decisions.js:27-34` | Attach via `tooltip` arrays to relevant options | Nash | Direct apply |
| T2-16 | "Partially clustered" judges example is observational IV | `decisions.js:188` | Drop/move the judges example | Neitzche | Direct apply |
| T2-17 | React hook-order risk in `Tooltip` | `app.jsx:41-52` | Move `return null` after `useEffect` | Neitzche | Direct apply (`haiku`) |
| T2-18 | `KatexMath` dead try/catch | `app.jsx:7-16` | Pick one strategy (throwOnError true+catch, or false and drop catch) | Neitzche | Direct apply |
| T2-19 | Runtime Babel + dev React in delivered HTML | `HTML:568` | Prefer built static bundle / vendored assets (bigger change) | Codex | **Agent:** Neitzche / `opus` — "Propose a minimal build step (esbuild/vite) to pre-compile the JSX and vendor React/KaTeX locally, so the delivered HTML has no runtime Babel or CDN dev React. Keep it a single self-contained HTML if possible. Outline the build + the trade-offs." |
| T2-20 | en-dash vs hyphen in author names | glossary vs recs | Standardize on en-dashes | Neitzche | Direct apply |
| T2-21 | `fwildclusterboot` seed; `pyfixest` CR2/CRV3 naming | `decisions.js:525,542,507` | Add `set.seed()`; verify `pyfixest` CRV naming | Nash | **Agent:** Nash / `sonnet` — "Confirm current `pyfixest` vcov syntax for cluster-robust + wild bootstrap: does it expose CR2, or only CRV1/CRV3? Give the exact `pf.feols(..., vcov=...)` call and the wild-bootstrap method. Also confirm `fwildclusterboot >= 0.13` global-seed behavior." |

---

## Convergence & downgrades
- **3-way convergence (settled):** T0-1, T0-2, T0-3, T0-4, T1-6, T1-12, T1-13, T2-8.
- **Downgraded / already-handled:** `impose_null=TRUE` is already the `fwildclusterboot` default (T2-9, explicitness only); DK-not-small-T caveat partially present at `decisions.js:564`; `cluster_randomization_pop` body needs no rewrite (only its code, T0-5); `S−1` not worth defining → delete (T2-4).

## Suggested implementation order
1. **Tier 0** (T0-1…T0-5) — run the three verification agents (T0-3, T0-4, T0-5) first, then apply.
2. **Tier 1 routing** (T1-1, T1-2, T1-3) — structural, then content (T1-4…T1-13).
3. **Tier 2** — cleanup pass; batch the `haiku`/direct-apply items, run the focused agents for T2-2/T2-8/T2-13/T2-19/T2-21.
